services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./hotel.db
      - SECRET_KEY=change-me-in-production
    volumes:
      - ./data:/code/data
